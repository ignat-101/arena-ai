{
  "name": "ton-trace",
  "version": "0.1.0",
  "description": "TON Trace — AML-визуализатор транзакций TON (граф связей, сущности, риск-скоринг, админка)",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node scripts/smoke.mjs && node scripts/smoke-admin.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
